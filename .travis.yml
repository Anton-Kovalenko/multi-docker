sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t antonkovalenko/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true antonkovalenko/react-test npm test

after_success:
  - docker build -t antonkovalenko/multi-client ./client
  - docker build -t antonkovalenko/multi-client ./nginx
  - docker build -t antonkovalenko/multi-client ./server
  - docker build -t antonkovalenko/multi-client ./worker
  #Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  #Take those images and put them to docker hub
  - docker push antonkovalenko/multi-client
  - docker push antonkovalenko/multi-nginx
  - docker push antonkovalenko/multi-server
  - docker push antonkovalenko/multi-worker

